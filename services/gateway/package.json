{
  "name": "@hunarbee/gateway",
  "version": "1.0.0",
  "private": true,
  "main": "./src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hunarbee/shared": "*",
    "cors": "*",
    "express": "*",
    "helmet": "*",
    "http-proxy-middleware": "*",
    "morgan": "*"
  }
}
